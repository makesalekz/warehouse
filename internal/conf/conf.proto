syntax = "proto3";
package kratos.api;

option go_package = "github.com/makesalekz/warehouse/internal/conf;conf";

import "google/protobuf/duration.proto";

message Bootstrap {
  Server server = 1;
  string db = 2;
  string nats = 3;
}

message Server {
  message GRPC {
    string network = 1;
    string addr = 2;
    google.protobuf.Duration timeout = 3;
  }
  GRPC grpc = 1;
}
