// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.calendaria.team/services/warehouse/ent/stockitem"
	"gitlab.calendaria.team/services/warehouse/ent/warehouse"
)

// StockItem is the model entity for the StockItem schema.
type StockItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID int64 `json:"tenant_id,omitempty"`
	// WarehouseID holds the value of the "warehouse_id" field.
	WarehouseID int64 `json:"warehouse_id,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID int64 `json:"product_id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity decimal.Decimal `json:"quantity,omitempty"`
	// MinQuantity holds the value of the "min_quantity" field.
	MinQuantity decimal.Decimal `json:"min_quantity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StockItemQuery when eager-loading is set.
	Edges        StockItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StockItemEdges holds the relations/edges for other nodes in the graph.
type StockItemEdges struct {
	// Warehouse holds the value of the warehouse edge.
	Warehouse *Warehouse `json:"warehouse,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WarehouseOrErr returns the Warehouse value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StockItemEdges) WarehouseOrErr() (*Warehouse, error) {
	if e.Warehouse != nil {
		return e.Warehouse, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: warehouse.Label}
	}
	return nil, &NotLoadedError{edge: "warehouse"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StockItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case stockitem.FieldQuantity, stockitem.FieldMinQuantity:
			values[i] = new(decimal.Decimal)
		case stockitem.FieldID, stockitem.FieldTenantID, stockitem.FieldWarehouseID, stockitem.FieldProductID:
			values[i] = new(sql.NullInt64)
		case stockitem.FieldCreatedAt, stockitem.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StockItem fields.
func (si *StockItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case stockitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			si.ID = int64(value.Int64)
		case stockitem.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				si.CreatedAt = value.Time
			}
		case stockitem.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				si.UpdatedAt = value.Time
			}
		case stockitem.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				si.TenantID = value.Int64
			}
		case stockitem.FieldWarehouseID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field warehouse_id", values[i])
			} else if value.Valid {
				si.WarehouseID = value.Int64
			}
		case stockitem.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				si.ProductID = value.Int64
			}
		case stockitem.FieldQuantity:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value != nil {
				si.Quantity = *value
			}
		case stockitem.FieldMinQuantity:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field min_quantity", values[i])
			} else if value != nil {
				si.MinQuantity = *value
			}
		default:
			si.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StockItem.
// This includes values selected through modifiers, order, etc.
func (si *StockItem) Value(name string) (ent.Value, error) {
	return si.selectValues.Get(name)
}

// QueryWarehouse queries the "warehouse" edge of the StockItem entity.
func (si *StockItem) QueryWarehouse() *WarehouseQuery {
	return NewStockItemClient(si.config).QueryWarehouse(si)
}

// Update returns a builder for updating this StockItem.
// Note that you need to call StockItem.Unwrap() before calling this method if this StockItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (si *StockItem) Update() *StockItemUpdateOne {
	return NewStockItemClient(si.config).UpdateOne(si)
}

// Unwrap unwraps the StockItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (si *StockItem) Unwrap() *StockItem {
	_tx, ok := si.config.driver.(*txDriver)
	if !ok {
		panic("ent: StockItem is not a transactional entity")
	}
	si.config.driver = _tx.drv
	return si
}

// String implements the fmt.Stringer.
func (si *StockItem) String() string {
	var builder strings.Builder
	builder.WriteString("StockItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", si.ID))
	builder.WriteString("created_at=")
	builder.WriteString(si.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(si.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", si.TenantID))
	builder.WriteString(", ")
	builder.WriteString("warehouse_id=")
	builder.WriteString(fmt.Sprintf("%v", si.WarehouseID))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", si.ProductID))
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", si.Quantity))
	builder.WriteString(", ")
	builder.WriteString("min_quantity=")
	builder.WriteString(fmt.Sprintf("%v", si.MinQuantity))
	builder.WriteByte(')')
	return builder.String()
}

// StockItems is a parsable slice of StockItem.
type StockItems []*StockItem
